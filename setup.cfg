[importlinter]
root_package = aether
include_external_packages = True

[importlinter:contract:layering]
name = agents -> skills -> memory -> models layering (CLAUDE.md)
type = layers
layers =
    aether.agents
    aether.skills
    aether.memory
    aether.models
